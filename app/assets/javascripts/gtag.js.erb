  document.addEventListener('turbolinks:load', event => {
    if (typeof gtag === 'function') {
      gtag('config', '<%=ENV['GA_TRACKING_ID']%>', {
        'page_location': event.data.url
      });
      console.log("gtag fired", event.data.url)
    }
  });
