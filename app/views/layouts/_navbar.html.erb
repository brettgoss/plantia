<nav id="navbar">
  <span id="brand">
    <a href="/">Plantia</a>
  </span>
  <span id="URLS">
    <%= link_to 'dashboard', dashboard_index_url %>
    <h1>Login</h1>

<%= form_tag '/login' do %>

  Email: <%= text_field_tag :email %>
  Password: <%= password_field_tag :password %>
  <%= submit_tag "Submit" %>

<% end %>
  </span>
</nav>